//https://open.kattis.com/submissions/15500926
#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int n,x,y=0,z;
    cin >> n >> x;
    while(cin >> z) y+=z;
    if(y>x) cout <<"Neibb";
    else cout <<"Jebb";
    return 0;
}
