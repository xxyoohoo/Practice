#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,b;
    cin >> a >> b;
    if(a==b && a>0) cout << "Even " << a+b;
    else if(a>b) cout << "Odd " << 2*a;
    else if(b>a) cout << "Odd " << 2*b;
    else cout << "Not a moose";
}
