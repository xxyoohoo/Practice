#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    string a;
    set<string> s;
    vector<string> v;
    cin >> n;
    while(n--)
    {
        cin >> a;
        if(s.count(a)==0)
        {
            v.push_back(a);
            s.insert(a);
        }
    }
    for (int i=0;i<v.size();++i) cout << v[i] << endl;
}
