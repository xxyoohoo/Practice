#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n,lens;
    string s1;
    set<string> s;
    cin >> n;
    while(n--)
    {
        cin >> s1;
        s.insert(s1);
    }
    vector<string> v(s.begin(), s.end());
    for (int i=0;i<v.size();++i) cout << v[i] << endl;
}
